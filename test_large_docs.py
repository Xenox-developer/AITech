#!/usr/bin/env python3
"""
Тестовый скрипт для проверки обработки больших документов
"""

import os
import sys
from pathlib import Path

# Добавляем текущую директорию в путь для импорта модулей
sys.path.append(str(Path(__file__).parent))

from ml import extract_topics_with_gpt, generate_summary, generate_flashcards

def create_test_text(size_category="small"):
    """Создает тестовый текст разного размера"""
    
    base_text = """
    Машинное обучение — это раздел искусственного интеллекта, который изучает методы построения алгоритмов, способных обучаться. Машинное обучение находит применение в задачах, где трудно или невозможно создать обычный алгоритм для решения задачи.

    Основные типы машинного обучения включают:

    1. Обучение с учителем (Supervised Learning)
    В этом типе обучения алгоритм обучается на размеченных данных, где для каждого входного примера известен правильный ответ. Основные задачи:
    - Классификация: предсказание категории или класса
    - Регрессия: предсказание числового значения
    
    Популярные алгоритмы: линейная регрессия, логистическая регрессия, деревья решений, случайный лес, метод опорных векторов (SVM), нейронные сети.

    2. Обучение без учителя (Unsupervised Learning)
    Алгоритм работает с неразмеченными данными и пытается найти скрытые закономерности. Основные задачи:
    - Кластеризация: группировка похожих объектов
    - Снижение размерности: уменьшение количества признаков
    - Поиск аномалий: выявление необычных паттернов
    
    Популярные алгоритмы: k-means, иерархическая кластеризация, DBSCAN, PCA, t-SNE, автоэнкодеры.

    3. Обучение с подкреплением (Reinforcement Learning)
    Агент обучается принимать решения в среде, получая награды или штрафы за свои действия. Цель — максимизировать суммарную награду.
    
    Ключевые концепции: состояние, действие, награда, политика, функция ценности.
    Применения: игры, робототехника, автономные системы, рекомендательные системы.

    Процесс машинного обучения включает несколько этапов:

    1. Сбор и подготовка данных
    Качество данных критически важно для успеха модели. Этапы:
    - Сбор данных из различных источников
    - Очистка данных: удаление дубликатов, обработка пропусков
    - Преобразование данных: нормализация, кодирование категориальных признаков
    - Разделение на обучающую, валидационную и тестовую выборки

    2. Выбор модели
    Выбор зависит от типа задачи, размера данных, требований к интерпретируемости:
    - Для табличных данных: градиентный бустинг, случайный лес
    - Для изображений: сверточные нейронные сети (CNN)
    - Для текста: рекуррентные сети (RNN), трансформеры
    - Для временных рядов: ARIMA, LSTM

    3. Обучение модели
    Процесс оптимизации параметров модели на обучающих данных:
    - Выбор функции потерь
    - Выбор оптимизатора (SGD, Adam, RMSprop)
    - Настройка гиперпараметров
    - Регуляризация для предотвращения переобучения

    4. Оценка модели
    Использование метрик для оценки качества:
    - Для классификации: точность, полнота, F1-мера, AUC-ROC
    - Для регрессии: MAE, MSE, RMSE, R²
    - Кросс-валидация для более надежной оценки

    5. Развертывание и мониторинг
    Внедрение модели в продакшн и отслеживание ее работы:
    - API для предсказаний
    - Мониторинг качества предсказаний
    - Обновление модели при деградации качества
    """
    
    if size_category == "small":
        return base_text
    elif size_category == "medium":
        # Повторяем текст 3 раза с вариациями
        return base_text + "\n\n" + base_text.replace("машинное обучение", "искусственный интеллект") + "\n\n" + base_text.replace("алгоритм", "метод")
    elif size_category == "large":
        # Создаем очень большой текст
        large_text = base_text
        for i in range(8):  # Повторяем 8 раз с модификациями
            modified_text = base_text.replace("машинное обучение", f"машинное обучение (раздел {i+1})")
            modified_text = modified_text.replace("данные", f"данные типа {i+1}")
            large_text += f"\n\n=== РАЗДЕЛ {i+1} ===\n\n" + modified_text
        return large_text
    
    return base_text

def test_processing(text, category):
    """Тестирует обработку текста"""
    print(f"\n{'='*50}")
    print(f"ТЕСТИРОВАНИЕ: {category.upper()}")
    print(f"Размер текста: {len(text)} символов")
    print(f"{'='*50}")
    
    try:
        # Тест извлечения тем
        print("\n1. Извлечение тем...")
        topics = extract_topics_with_gpt(text)
        print(f"   ✓ Найдено тем: {len(topics.get('main_topics', []))}")
        print(f"   ✓ Связей: {len(topics.get('concept_map', {}).get('relationships', []))}")
        
        # Тест создания резюме
        print("\n2. Создание резюме...")
        summary = generate_summary(text)
        print(f"   ✓ Длина резюме: {len(summary)} символов")
        
        # Тест создания флеш-карт
        print("\n3. Создание флеш-карт...")
        flashcards = generate_flashcards(text)
        print(f"   ✓ Создано карточек: {len(flashcards)}")
        
        # Показываем примеры результатов
        if topics.get('main_topics'):
            print(f"\n   Пример темы: {topics['main_topics'][0]['title']}")
        
        if flashcards:
            print(f"   Пример карточки: {flashcards[0]['q'][:50]}...")
        
        print(f"\n✅ ТЕСТ {category.upper()} ПРОЙДЕН УСПЕШНО")
        
    except Exception as e:
        print(f"\n❌ ОШИБКА В ТЕСТЕ {category.upper()}: {str(e)}")
        return False
    
    return True

def main():
    """Основная функция тестирования"""
    print("🧪 ТЕСТИРОВАНИЕ ОБРАБОТКИ БОЛЬШИХ ДОКУМЕНТОВ")
    print("=" * 60)
    
    # Проверяем наличие API ключа
    if not os.environ.get('OPENAI_API_KEY'):
        print("❌ ОШИБКА: Не установлен OPENAI_API_KEY")
        print("Установите переменную окружения:")
        print("export OPENAI_API_KEY='your-api-key-here'")
        return
    
    results = []
    
    # Тест 1: Малый текст (до 15К символов)
    small_text = create_test_text("small")
    results.append(test_processing(small_text, "малый текст"))
    
    # Тест 2: Средний текст (15К-50К символов)
    medium_text = create_test_text("medium")
    results.append(test_processing(medium_text, "средний текст"))
    
    # Тест 3: Большой текст (50К+ символов)
    large_text = create_test_text("large")
    results.append(test_processing(large_text, "большой текст"))
    
    # Итоговый отчет
    print(f"\n{'='*60}")
    print("📊 ИТОГОВЫЙ ОТЧЕТ")
    print(f"{'='*60}")
    
    test_names = ["Малый текст", "Средний текст", "Большой текст"]
    for i, (name, result) in enumerate(zip(test_names, results)):
        status = "✅ ПРОЙДЕН" if result else "❌ ПРОВАЛЕН"
        print(f"{name}: {status}")
    
    success_rate = sum(results) / len(results) * 100
    print(f"\nОбщий процент успеха: {success_rate:.1f}%")
    
    if all(results):
        print("\n🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Система готова к обработке больших документов.")
    else:
        print("\n⚠️  Некоторые тесты провалены. Проверьте конфигурацию.")

if __name__ == "__main__":
    main()